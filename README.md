# Model_Fairness_Comparison
Developed an interactive ML fairness dashboard to compare Logistic Regression, Decision Tree, and Random Forest models across multiple classification thresholds. Evaluated model performance using accuracy, precision, recall, confusion matrices, and fairness metrics such as demographic parity, equal opportunity, equalized odds, and disparate impact.
